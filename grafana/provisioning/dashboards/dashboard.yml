apiVersion: 1

providers:
  - name: FlaskAppDashboards
    orgId: 1
    folder: "Flask Monitoring"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
